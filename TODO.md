- [ ] Identify why pages are not rendering (route/template/static mismatch)
- [ ] Fix `main.py` to correctly render men/women using consistent TemplateResponse parameters
- [ ] Start FastAPI server with `uvicorn` and verify endpoints `/`, `/men`, `/women`
- [ ] If still broken, capture terminal traceback and browser URL/response status

